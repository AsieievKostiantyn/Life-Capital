name: "CI: Pull Request"

on:
  pull_request:

jobs:
  jobs:
  security-check:
    name: "Security check"
    permissions:
      contents: read
      pull-requests: write
    uses: ./.github/workflows/_ci-security-check.yml